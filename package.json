{
  "name": "logans-geometrie-crash",
  "version": "6.11.0",
  "private": true,
  "description": "Logan's Geometrie Crash als separates Android-Projekt via Capacitor",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "node scripts/build-web.mjs",
    "sync:android": "npx cap sync android",
    "android:open": "npx cap open android",
    "apk:debug": "npm run build && npm run sync:android && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run build && npm run sync:android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.0"
  }
}
